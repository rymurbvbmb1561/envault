package cli

// This file registers the audit command group into the shared command registry.
// The actual command implementation lives in audit.go.

var _ = commandRegistry // ensure registry is initialised
